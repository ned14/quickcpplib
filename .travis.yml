language: cpp
sudo: false
os:
 - linux
branches:
  only:
    - master
notifications:
  email:
#    recipients:
#      - nialldouglas14@gmail.com

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
#    - llvm-toolchain-precise-3.8  ## not currently working due to bad source signing key
    packages:
    - g++-6

before_install:
 - git submodule update --init --recursive

script:
 - cd test
 - ./withgcc.sh
 - g++-6 -std=c++14 -Wall -Wextra -o atuple -O3 atuple.cpp
 - ./atuple
 - g++-6 -std=c++14 -Wall -Wextra -o ringbuffer_log -O3 ringbuffer_log.cpp
 - ./ringbuffer_log
 - g++-6 -std=c++14 -Wall -Wextra -o spinlock_tribool -O3 spinlock_tribool.cpp
 - ./spinlock_tribool
 
after_success:
# - bash -x ./update_coveralls.sh `pwd`

after_failure:
